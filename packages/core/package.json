{
  "name": "@typed-router/core",
  "version": "1.0.0",
  "author": "Maxim Kiselev",
  "license": "ISC",
  "type": "module",
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "module": "build/index.js",
  "scripts": {
    "dev": "rimraf build && tsc --build tsconfig.json --watch",
    "build": "rimraf build && tsc --build tsconfig.json && npm install --workspaces",
    "start": "ts-node-esm --experimental-specifier-resolution=node build/index.js"
  },
  "devDependencies": {
    "@types/express": "^4.17.21"
  },
  "dependencies": {
    "express": "^4.18.2",
    "path-to-regexp": "^6.2.1",
    "qs": "^6.11.2"
  }
}
